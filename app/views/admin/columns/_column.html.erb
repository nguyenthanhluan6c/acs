<tr>
  <%= form_for [:admin, column] do |f| %>
    <td class="stt"></td>
    <td><%= f.text_field :display_name %></td>
    <td><%= f.text_field :name %></td>
    <td><%= f.text_field :table_expression %></td>
    <td><%= f.text_field :index %></td>
    <td>
      <%= f.submit t(".save"), class: "btn btn-primary" %>
      <%= link_to t(".delete"), [:admin, column], method: :delete, class: "btn btn-danger" %>
    </td>
  <% end %>
</tr>
